---
title: svg filter effects
---

import Playground from "../../components/Playground"
import { FilterEffects } from "../../components/PostAssets/svg-fe"
import gsap from "gsap"

# SVG Filter Effects

## Turbulence

<Playground scope={{ FilterEffects, gsap }}>
  {`() => {

    useEffect(() => {
        const feTurbulence = document.querySelector('#fe > feTurbulence')
        const feDisplacementMap = document.querySelector('#fe > feDisplacementMap')

        const progress = { current: 0 }
        gsap.to(progress, {
            current: 1,
            duration: 3,
            yoyo: true,
            ease: 'none',
            repeat: -1,
            onUpdate() {
                feTurbulence.setAttribute('baseFrequency', 0.005 + progress.current / 200)
            }
        })
    }, [])

    return <div style={{width: 300, height: 200, overflow: 'hidden'}}>
        <svg style={{display: 'none'}}>
        <filter id="fe">
            <feTurbulence result="fe-turbulence" baseFrequency="0.02" numOctaves="2" />
            <feDisplacementMap xChannelSelector="R" yChannelSelector="G" result="fe-displacement-map" scale="16" in="SourceGraphic"/>
        </filter>
        </svg>
        <img src="/fire.webp" alt="" style={{ filter: "url(#fe)", margin: -16, maxWidth: 'unset'}} width="332" height="232"/>
    </div>

}`}

</Playground>
